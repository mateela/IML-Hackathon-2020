model.py - classifier file. Serves for using the trained classifier.
trainer.py - The code we used to trained the model.
dataloader.py - The code we used to load the data and for preprocessing.
model - This file contains classifier's data used for prediction.
project.pdf - project description and summery.
USER.txt - contains the logins and IDs of all the team members.
requirements.txt -  the required packages for your virtual environment.